#include <iostream>
#include <vector>
#define fo(x) freopen(#x".in", "r", stdin); freopen(#x".out", "w", stdout);

using std::cin;
using std::cout;
using std::vector;

int main() {
    
#ifndef LOCAL
    fo(lis)
#endif
    cout << "No";
}